[phases.setup]
nixPkgs = ["nodejs_20", "php", "composer"]

[phases.install]
cmds = [
  "composer install",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
